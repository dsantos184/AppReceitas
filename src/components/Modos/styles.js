import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container:
    {
        flex: 1,
    },

    modos:
    {
        fontSize: 16,
        marginBottom: 10,
    }
});

export default styles;